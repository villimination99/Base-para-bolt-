<section class="section-padding page-main">
  <div class="container container--narrow">
    <article>
      <h1 class="section-title">{{ page.title }}</h1>
      <div class="rte page-content">{{ page.content }}</div>
    </article>
  </div>
</section>

{% schema %}
{ "name": "Página", "settings": [] }
{% endschema %}
